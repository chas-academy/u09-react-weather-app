import React from 'react';

const Header = props => {
    return (
        <article>
            <header className = "text-center">
                <h1>Weather App</h1>
            </header>
        </article>
    );
}

export default Header;